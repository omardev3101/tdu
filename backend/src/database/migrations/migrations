'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    await queryInterface.addColumn('members', 'balance_retroactive', {
      type: Sequelize.DECIMAL(10, 2),
      allowNull: true,
      defaultValue: 0.00,
    });
  },

  down: async (queryInterface) => {
    await queryInterface.removeColumn('members', 'balance_retroactive');
  }
};